int maxSubArray(int* nums, int numsSize) {
    int sum = nums[0];

    int maxEnd = nums[0];

    for(int i=1; i<numsSize; i++)
    {
        maxEnd = (maxEnd+nums[i]>nums[i] ? maxEnd+nums[i] : nums[i]);
        sum = (sum>maxEnd ? sum : maxEnd);
    }

    return sum;
}
